# Shell Scripting Basics

A "shell script" (`.sh` files) is a plain text file containing a series of commands. Instead of typing the same commands repeatedly, you save them to a file and run it instead.

